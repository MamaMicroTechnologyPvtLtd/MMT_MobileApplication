# PostgreSQL connection string
DATABASE_URL=postgres://postgres:postgres@localhost:5432/mmt

# Auth
JWT_SECRET=change-me-to-a-long-random-secret
JWT_EXPIRES_IN=30d

# Server
PORT=4000

# File uploads (quotation PDFs, invoices, delivery docs)
UPLOAD_DIR=uploads
